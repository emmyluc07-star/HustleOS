{
  "name": "hustleos",
  "version": "2.2.0",
  "description": "Cyberpunk productivity timer with native system integration",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Build complete'",
    "cap:add": "npx cap add",
    "cap:sync": "npx cap sync",
    "cap:run": "npx cap run",
    "cap:build": "npm run build && npx cap sync"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.5.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@capacitor/android": "^5.5.1",
    "@capacitor/app": "^5.0.6",
    "@capacitor/core": "^5.5.1",
    "@capacitor/local-notifications": "^5.0.6"
  }
}
